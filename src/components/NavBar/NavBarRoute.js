import React, {useState} from "react";


export const NavBarRoute = (category) => {
    const [catBlog, setCatBlog] = useState([])
    const changeCat = () => {
        if (category === 1) {

        }
    }
    return
}